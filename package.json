{
  "name": "your-project-name",
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir -p dist && rsync -av --exclude='dist' --exclude='.git' . dist"
  },
  "dependencies": {},
  "devDependencies": {}
}
